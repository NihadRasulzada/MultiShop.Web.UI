@model List<ResultProductDto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/AdminLayout/_AdminLayout.cshtml"; 
    int count = 0;
}

<section class="content container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="chart-box">
                <div class="bs-example" data-example-id="bordered-table">
                    <table class="table table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Ürün Sekili</th>
                                <th>Ürün Adı</th>
                                <th>Ürün Qiymeti</th>
                                <th>Sil</th>
                                <th>Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <td scope="row">@count</td>
                                    <td><img src="@item.ImageLink" style="width: 50px; height: 50px;" /></td>
                                    <td>@item.Name</td>
                                    <td>@item.Price</td>
                                    <td><a href="/Admin/Product/Delete/@item.Id" class="btn btn-danger">Sil</a></td>
                                    <td><a href="/Admin/Product/Update/@item.Id" class="btn btn-success">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Product/Create/" class="btn btn-primary">Yeni Ürün Girişi</a>
                </div>
            </div>
        </div>
    </div>
</section>
